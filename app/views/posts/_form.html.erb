<%= simple_form_for(@post, html: { class: 'form-horizontal' }) do |f| %>

  <%= f.input :title %>
  <%= f.input :text %>
  <%= f.text_field :all_tags, placeholder: "Put tags with a comma" %> <br>
  <%= f.button :submit %>
<% end %>
